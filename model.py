import numpy as np

"""
model.py Contains API for training and evaluating the model.
"""


class train:
    """
    Compile layers of model, put x_train and y_train for training, define loss function
    and append all of the parameters and gradients of each layer in the dictionary
    to compute updated parameters.
    """
